/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'images.pexels.com', 'via.placeholder.com', 'assets.site-static.com', 'cdn-images.wework.com'],
  },
}

module.exports = nextConfig

